import React from "react";
import Layout from "../../components/Layout";

const NotFound = () => {
  return <Layout title="Ops, conteúdo não encontrado!">Parece que o conteúdo que você está buscando não existe!</Layout>;
};

export default NotFound;
